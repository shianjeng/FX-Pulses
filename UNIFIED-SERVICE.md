# FX Pulse 2.5.0 统一数据服务

工具栏、插件原生悬停与可选油猴界面共享插件后台。正式市场数据源默认 Alpha Vantage，API Key 只配置在后端。ECB、BoC、Fed、BoJ、PBOC 官方参考价继续作为带日期和机构标签的补充。

## 升级

保留数据库和现有 .env，设置 FX_PROVIDER=alpha_vantage 及 ALPHA_VANTAGE_API_KEY 后重建后端。重新加载 2.5.0 插件，更新可选油猴脚本，开启网页悬停权限并刷新网页。完整命令见 [README](README.md#quick-start)。

## 请求路径

油猴通过页面事件桥接请求快照或固定格式的官方货币对。插件内容脚本转交后台，后台检查悬停是否启用、合并并发请求、缓存市场快照 60 秒和官方结果 10 分钟。前端请求不会触发上游采集。

桥接不接受任意 URL、历史接口、配置读取、强制刷新或写入操作，也不返回后端地址及 API Key。桥接事件对宿主网页可见，并不提供页面内的数据真实性保证；原生插件界面使用隔离脚本与封闭 Shadow DOM。

油猴界面保留单独的目标币种、外观与站点校准设置；数据服务统一不代表设置已迁移。连接期间原生悬停暂时让位。禁用或缺少插件时油猴不会回退到其他汇率源。

## 数据口径

直接报价优先，其次倒数。没有市场报价时读取同一后端最新官方参考价，显示机构及参考日期。官方汇率不是 Alpha Vantage 实时报价。多个界面会在各自刷新时读取共享数据，不保证逐毫秒同时重绘。

配置默认仍采集三个市场货币对；2.4.0 的广泛官方货币覆盖继续保留。增加 Alpha Vantage 货币对必须重新评估采集周期和 API 配额。模拟数据仅在显式 FX_PROVIDER=mock 时用于开发。
