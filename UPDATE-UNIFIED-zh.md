# FX Pulse 统一服务版：安装与迁移

本包包含此前的 bug 修复、工程加固、方案 B Logo，以及新的工具栏/悬停统一服务。
这是仓库累计更新包，不包含 API key、.env、数据库或 node_modules。先备份本地改动，再按相同路径合并文件，不要整体删除原文件夹。

## 已安装上一版的用户

1. 将包中同名文件合并到现有项目。extension 文件夹内是完整的新版插件文件。
2. 打开 chrome://extensions，重新加载 FX Pulse，确认版本显示 2.1.0。
3. 点击插件设置，确认后端地址和连接正常。
4. 开启“网页悬停换算”，允许浏览器请求的网页访问权限。
5. 在 Tampermonkey 中停用旧 FX Pulse Hover 脚本。以后只维护 Chrome 插件。
6. 刷新要使用的网页，再将鼠标停在金额文字上。

如果之前的后端修复已经安装，本轮合并无需改动 API key、数据库或重新构建后端。
如果此前修复尚未安装，在项目根目录运行：

```sh
docker compose up -d --build api collector
```

## 共用与保留

- 工具栏和悬停读取同一 FastAPI 后端报价，使用统一后台请求与一分钟缓存。
- 同一份后端地址、语言和自选；悬停目标币种、大小、模式也在插件设置页管理。
- 保留方案 B 图标与此前修复。没有配置/采集的货币对不会用另一家数据源偷偷替代。
- 原有插件设置保留；旧油猴里的站点币种校准需在新版重新设置，不自动导入。
- 旧油猴源码仅供开发和解析器回归测试，不要重新安装。旧版任意自定义 CSS 与调试菜单没有移入正式插件。

## 验证和限制

前端/服务测试 47 项通过，后端 31 项通过；ESLint、Ruff、旧解析器和 DOM 冒烟检查通过。
尚未执行真实 Chrome、Docker 与真实 Alpha Vantage 端到端验收，请按 docs/SCREENSHOTS.md 做本机验证。
不支持 iframe、file://、Chrome 内部页、商店等受保护页面。默认悬停关闭，开启后需要刷新已打开的网页。

详细架构与隐私说明见 UNIFIED-SERVICE.md。
你引用的完整首用诊断向导、手续费换算、三币批量换算与官方参考价偏差展示不在本次合并范围，未声称已经实现。

## 上传 GitHub

在 GitHub Desktop 检查差异后 Commit、Push origin；本包尚未替你提交或推送。
注意不要遗漏 .github/workflows/ci.yml。macOS 可用 Command + Shift + . 显示隐藏文件夹。
