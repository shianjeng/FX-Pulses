# 设计与工程风险处理记录

> 本文记录前一轮加固。后续已完成方案 B Logo 替换与插件/悬停统一；当前架构与限制以 [UNIFIED-SERVICE.md](docs/archive/UNIFIED-SERVICE.md) 为准。下文关于两套独立运行入口的描述仅为历史记录。

本轮基于上一轮 7 项 bug 修复版继续修改。基线为 shianjeng/FX-Pulses main 提交 8e4b49162a2555d1de974933b3a97cef276fb4c0，不包含图标重设计。

## 处理清单

| 风险 | 本轮处理或验证 |
| --- | --- |
| 网页可以修改悬停脚本内部状态 | 移除 unsafeWindow 权限与公开 __fxph 接口，使用封闭 Shadow DOM；不信任网页 localStorage；诊断用汇率副本深复制 |
| 长期开着网页，缓存不再刷新 | 悬停、返回可见页面和可见卡片定时检查 TTL；单页请求合并；失败后一分钟退避 |
| 负数金额误读为正数 | 拒绝负号及会计括号金额；换算函数验证数值范围 |
| 模拟历史混入真实行情 | 现有查询已按 provider 过滤；增加真实与模拟数据混存的回归测试 |
| 快速切换币种导致旧图覆盖新图 | 保留请求版本保护，补充延迟响应测试；加载中清空旧统计；历史加载失败显示错误而不是伪装成空历史 |
| 两个入口的数据、设置、权限不一致 | 保留独立架构；插件设置页增加安装入口和中英日说明；悬停卡片显示来源、数据时间与抓取时间；文档明确不是同一报价口径 |
| 小号卡片隐藏币种校准 | 移除隐藏两个币种选择器的规则；更新内嵌主题并验证 |
| 图表、缺数据、模拟标识与复制行为误导 | 累计保留单点/真实时间折线修复、24h 标签、模拟提示和复制兜底；无涨跌对比改用中性颜色；悬停无缓存时不再代入示例报价，复制失败提供可选文本 |
| API 启动等待上游、每进程采集 | 当前 API 生命周期不发网络请求；采集器独立进程及共享文件锁保持不变，增加生命周期测试 |
| 异常被吞、日志泄露密钥 | 保留采集失败日志；Alpha Vantage 网络异常转换为不含请求地址的错误，测试验证日志不含 key |
| CI 仅测试后端 | 加入 Node 前端、悬停解析及 DOM 冒烟任务，与后端测试一起运行 |

## 验证结果

- `npm test`：30 项通过。
- `npm run lint`：通过。
- `node userscript/node-check.mjs`：全部解析用例通过。
- `node userscript/dom-smoke.cjs`：全部 DOM 契约检查通过。
- 后端 `pytest -q`：31 项通过；有一条测试依赖的弃用警告，不影响通过。
- 后端 `ruff check .`：通过。

> 后注：`userscript/node-check.mjs` 之后已移除，这些解析用例现在由 `npm test` 覆盖；DOM 契约检查用 `npm run test:userscript` 运行。全部检查可用 `make check && make test`。

## 仍需知晓的边界

此次未运行真实 Chrome/Tampermonkey、Docker、GitHub Actions 或真实 Alpha Vantage 端到端测试；已更新视觉检查脚本，但当前环境未执行它。

悬停与插件没有统一设置/语言存储；悬停工具仍是独立日更参考汇率工具。不同标签页没有全局请求锁。后端共享文件锁适用于使用同一锁文件/卷的单机部署，不是跨主机分布式锁；API 限流仍为单进程限制。公网或多实例部署需要单独设计认证、代理限流与集中式采集锁，不能直接将本地配置当作公网生产方案。

封闭 Shadow DOM 和脚本沙箱用于隔离内部接口，不保证恶意网页无法伪造自己的显示内容。请核对来源和时间，汇率换算不包含银行手续费或点差。
